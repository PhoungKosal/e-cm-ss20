import type {NextConfig} from "next";

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: ['fakestoreapi.com', 'example.com', 'tinh-serey.vercel.app', 'images.unsplash.com'],
    },
};
export default nextConfig;

